#/bin/bash

bin_dir=$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")
base_dir=$(dirname $bin_dir)
python_dir=$base_dir/.venv/bin
script_dir=$base_dir/src

export PYTHONNOUSERSITE=1
$python_dir/python3.9 $script_dir/Trainer.py "$@"
